# Environnement — copier vers .env.local et remplir

# "production" | "staging" | "development"
# production = indexation SEO activée (meta robots index,follow)
# Sur Vercel prod, VERCEL_ENV=production suffit aussi si cette variable est absente.
NEXT_PUBLIC_ENV=production

# URL canonique du site (sans slash final)
NEXT_PUBLIC_SITE_URL=https://www.depannagescooter.com

# Google Analytics 4 — measurement ID (ex. G-XXXXXXXXXX)
NEXT_PUBLIC_GA_ID=

# Formulaire de contact — webhook optionnel (Slack, n8n, etc.)
CONTACT_WEBHOOK_URL=

# Email de réception des demandes de rappel
CONTACT_EMAIL=scooterdepannage@gmail.com

# Secret pour rate limiting (générer une chaîne aléatoire, ex. openssl rand -hex 32)
CONTACT_RATE_LIMIT_SECRET=TODO-generer-un-secret

# SMTP — ex. Gmail App Password, OVH, Brevo SMTP relay
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
